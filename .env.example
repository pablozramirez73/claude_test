# For docker-compose.misura.yml (the full MISURA stack — db + backend +
# frontend, run together from the repo root). Copy to ".env" and adjust.
# Each app also has its own narrower .env.example for standalone use:
#   apps/misura-backend/.env.example
#   apps/misura-miniapp/.env.example

MISURA_WEB_PORT=8091
BACKEND_PORT=8099
POSTGRES_PORT=5442

POSTGRES_DB=misura
POSTGRES_USER=misura
POSTGRES_PASSWORD=misura

DJANGO_SECRET_KEY=insecure-dev-key-change-me
DJANGO_DEBUG=false
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_CORS_ALLOWED_ORIGINS=http://localhost:8091,http://localhost:5183
PROFILE_HASH_SALT=misura-dev-salt-change-me

# Local LLM for style/fit advice (profiles/llm.py) — talks to an Ollama
# instance YOU already run on this machine; nothing here installs or pulls
# one. The backend container reaches it via host.docker.internal (see
# docker-compose.misura.yml); override OLLAMA_BASE_URL only if your Ollama
# listens somewhere non-default.
OLLAMA_MODEL=gemma4:latest
OLLAMA_TIMEOUT_SECONDS=60
# OLLAMA_BASE_URL=http://host.docker.internal:11434

# Only needed for the optional "cloudflared" service (public HTTPS URL for
# Telegram, e.g. misura.syntaxnode.work) — see the setup steps in
# docker-compose.misura.yml. This IS a real secret once you generate one:
# get it from `cloudflared tunnel token <name>`, put the real value only in
# your own untracked .env, never here, never committed.
CLOUDFLARE_TUNNEL_TOKEN=
